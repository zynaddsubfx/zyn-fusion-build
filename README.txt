These are the build scripts used to generate the Zyn-Fusion packages.
At the time that they are uploaded to github they aren't really all that
useful to anyone, but once the rest of Zyn-Fusion is opened up, then these
repos should make automated package building relatively easy.
